[project]
name = "dwei-robot-project"
version = "0.1.0"
description = "python simulation for lqr to enable balancing of the robot"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.2",
    "scipy>=1.17.1",
]
